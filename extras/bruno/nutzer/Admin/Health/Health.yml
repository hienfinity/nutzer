info:
  name: Health-Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{healthUrl}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.status
      operator: eq
      value: up

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
