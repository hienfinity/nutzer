info:
  name: Nicht-vorhandene ID 999999
  type: http
  seq: 2

http:
  method: GET
  url: "{{restUrl}}/999999"
  headers:
    - name: Authorization
      value: Bearer {{token}}

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
